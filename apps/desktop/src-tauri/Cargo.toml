[package]
name = "forge_desktop"
version = "0.1.0"
description = "FORGE operator workspace shell"
authors = ["FORGE"]
license = "MIT"
repository = ""
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.3", features = [] }

[dependencies]
tauri = { version = "2.1.0", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
