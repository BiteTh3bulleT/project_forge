# Merge Instructions

Do not overwrite repo files blindly.

## Safe merge order

1. Add this pack under `docs/prompt_packs/forge-k-online/`.
2. Review `.cursor/rules` before copying into active repo rules.
3. Merge `AGENTS.md` content manually if needed.
4. Keep phase prompts in their own folder.
5. Use one prompt at a time.

## Files to treat carefully

- `AGENTS.md`
- `.cursor/rules/*`
- `docs/adr/*`
- `docs/status/*`
