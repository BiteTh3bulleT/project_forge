# Universal FORGE-K Online Agent Header

You are working inside the FORGE repository.

Your job is to execute exactly one bounded phase toward getting FORGE-K fully online.

## Binding doctrine

- NixOS owns the host.
- Gateway owns tool execution.
- Modelruntime owns runtime drivers.
- FORGE-K owns semantic truth flow only after explicit migration.
- The operator owns dangerous authority.
- Simulator services are not live daemon authority unless an explicit migration phase promotes one narrow seam.

## Required first actions

1. Read `AGENTS.md`.
2. Read `docs/status/current_authority_sources.md`.
3. Read `docs/reviews/current_phase_status.md`.
4. Read `docs/status/implementation_matrix.md`.
5. Inspect files relevant to this phase.
6. Produce a short plan before editing.

## Execution rules

- Use the three-pass execution loop.
- Do not output code in chat; modify files.
- Do not run broad refactors.
- Do not change route/API behavior unless explicitly requested.
- Add tests with code changes.
- Update docs/status if status changes.
- Record validation commands and results.

## Final response contract

Return:

- Summary
- Files changed
- Authority impact
- Tests run
- Tests not run and why
- Rollback path
- Remaining blockers

## WHAT NOT TO DO

- Do not wire full FORGE-K into live daemon.
- Do not import simulator Kernel/Courthouse/Context Compiler/Runtime services into live paths.
- Do not bypass gateway, permissions, lanes, audit, or approvals.
- Do not treat model output, Qdrant scores, Redis values, KV cache, or raw chat as canonical truth.
- Do not mutate NixOS host config directly from FORGE-K.
