# FORGE Implementation Matrix (Current Live Authority Snapshot)

Observed against this branch on 2026-08-16.

Top note: this matrix tracks legacy/live AI-OS implementation status and current daemon authority paths. It is not the FORGE-K simulator phase matrix. For current phase status and FORGE-K simulator/live authority boundaries, see `docs/reviews/current_phase_status.md`, `docs/status/current_authority_sources.md`, and ADR 0005 (`docs/adr/0005-forge-k-simulator-vs-live-authority.md`).

Status values: `real`, `partial`, `legacy-boundary`, `blocked`, `scaffold`, `deferred`.

| Subsystem | Current authoritative path | Status | Evidence | Main remaining gap |
|---|---|---|---|---|
| Tool execution | `/api/gateway/invoke` -> `gateway.Execute` | real | `internal/gateway/service.go`, gateway tests | keep gateway-only ingress discipline |
| Legacy adapter side door | route removed | resolved | `server.go`, `server_adapters_test.go` | `/api/adapters/{id}/invoke` no longer wired; removed-route behavior tested as `404` |
| Model runtime governance (M3/M4) | `/forge/models*` + `modelruntime.Service` (with gated `/v1/*` compatibility) | partial (implemented) | `services/core/internal/modelruntime/*`, `services/core/internal/api/model_runtime.go`, `services/core/internal/api/model_runtime_bridge.go`, `services/core/internal/config/config.go`, gateway `model.*` policy aliases | Remaining work is hardening/supervision: stronger backend/process control, deeper scheduling/backpressure, cancellation/usage accounting hardening, and operator visibility |
| Semantic mutation Kernel | `forgekernel.Kernel` (default) -> authenticated authorization proof -> K-owned `DurablePort` -> Control Lane SQLite implementation | partial cutover (K20E) | unique assembly-time `forge.core` principal; bearer/loopback origin tests; registry/capability/approval proof and plan binding; exact request/plan seals; typed receipts; atomic mutation/journal/provenance/request-bound audit outbox/idempotency proof; authorization-bound no-recommit replay; tamper and rollback tests | migrate remaining subsystem/direct-writer authority, extract Control Lane policy/apply/SQLite details, deliver external audit projections, and retire adapter/v1 rollback |
| Retired memory observation/link/repair mutation | production FORGE-K `MATERIALIZE_ADMITTED_EVIDENCE` / `REVISE_MEMORY_EVIDENCE`; legacy reads only | resolved (K20H) | terminal `410 Gone` routes, fail-closed Go seams, proposal-only repair, callsite/SQL/static guards | add future evidence endpoints only through authenticated production Kernel ingress; never reconnect legacy writers |
| Admitted Memory Palace evidence | production Kernel -> Court-bound materialize/revise actions -> immutable evidence + append-only linear supersession | partial cutover (K20H) | exact scope/hash/current-ruling validation; authenticated user/service proof; atomic provenance/journal/audit/idempotency receipt; replay, rollback, immutability, concurrency, backup-inspection tests | finish remaining Memory Palace readers and whole-store recovery; legacy observations remain untrusted |
| Semantic Algebra | production Kernel `COMPUTE_SEMANTIC_DIFF` -> pure `forgekernel/semanticdiff` -> immutable SQLite operation/result/noncanonical object | partial cutover (K20I) | exact admitted K20H source authority, deterministic diff vectors, sealed source/result commitments, replay/conflict/rollback/immutability and backup-inspection tests | add narrow authenticated ingress; migrate additional operators only as separate deterministic contracts; extract temporary Control Lane adapter |
| Context Compiler | legacy `COMPILE_CONTEXT`; pure future authority contract at `forgekernel/contextcompile` | pure contract / not live integrated (K20I lane C) | sealed fixed-point policy, exact source/candidate/outcome/head commitments, golden/permutation/bounds/tamper/fuzz tests | make compile K-only, add consistent read manifest, Kernel decision ownership, CAS persistence/receipt commitments, and retire legacy compiler authority |
| Backup restore authority | `/api/backup/restore` -> deterministic `backup.Service.InspectBundle` only | blocked apply / real inspection | stable non-dry fail-close tests, manifest/count/checksum/tamper tests, and production callsite static guard | build daemon-stopped, whole-store, chain-verified recovery; do not re-enable row-level live merge |
| Full-backup K20C/K20D coverage | export Court tables, journal head/events, audit outbox, and idempotency proof | real export / no restore | `backup/service.go`, `section_mappings.go`, inspection tests | prove offline recovery preserves immutable chain/proof identity |
| Utility evidence | production Kernel -> utility action -> immutable event + separate noncanonical projection | partial cutover (K20G) | utility action/store/schema/API/retrieval tests; replay and rollback guards | add exact scoped batch retrieval-outcome evidence; keep legacy writers retired |
| Retrieval run evidence | live retrieval compute -> production Kernel `RECORD_RETRIEVAL_EVIDENCE` -> atomic SQLite durable port | partial cutover (K20G) | exact source-root scope, authenticated attribution, deterministic object plan, immutable run/results/reasons, journal/audit/idempotency proof, retry/tamper/rollback/lifecycle tests | search and embeddings remain live-service-owned; add safe whole-store recovery and migrate remaining retrieval-adjacent writers |
| Memory acceleration projection | production Kernel `REBUILD_MEMORY_ACCELERATION` -> deterministic scoped manifest -> atomic SQLite projection swap | governed but source-blocked (K20G) | pure manifest tests, CAS/tamper/rollback tests, scoped reader tests, legacy-writer AST guard | add Courthouse-admitted scoped memory-source syscall; never activate legacy/unscoped observations |
| Restore-outcome feedback | production FORGE-K append-only event; original evidence plus separate read projection | partial cutover (K20G) | exact-scope API/action tests, immutable schema, replay and journal-rollback tests | keep projection rebuildable; include only through safe whole-store recovery |
| Autonomy persistence safety | SQLite-backed autonomy repos + persistence gate | mostly complete | autonomy policy/runner tests | trace/audit visibility still partial |
| Rule-agent layer | propose-only runtime with 2 agents | partial (explicitly narrow) | `rule_agents.go`, safety guard tests, `docs/architecture/rule_based_agents.md` | broader deterministic agents deferred until signal, policy, test, and trace coverage exists |
| Desktop/backend mutation boundary | desktop -> backend `/api/*` | real | `apps/desktop/src/lib/api.ts`, API server wiring, Audit page trace lookup rendering, chat/gateway/job/approval/artifact/workbench/events/lineage Audit pivots | trace/explain UI has a read-only Audit page authority-chain summary with direct pivots from existing operator surfaces when trace, correlation, or job context is available; artifact/provenance/journal object-id lookup remains future backend work |
| Remote gateways | `/api/remote/telegram`, `/api/remote/discord`, `/api/telegram/status`, `/api/discord/status`, optional Discord bot gateway | partial | `services/core/internal/api/remote.go`, `telegram_gateway_service.go`, `discord_gateway_service.go`, `discord_gateway_server.go`, `docs/REMOTE_ACCESS.md`, `docs/architecture/discord_integration_layer.md` | Telegram/Discord ingress and status are real when configured; Discord command coverage remains intentionally narrow and unimplemented commands return `NOT_IMPLEMENTED`; no broader Discord workflow authority without separate tests/docs |
| JS/TS validation surface | root `test:js`/`lint:js`/`validate:js` + desktop Vitest/typecheck/build | partial (improved) | `package.json`, `apps/desktop/package.json` | lint is TypeScript-only; no ESLint lane or non-desktop package tests yet |
| Nix foundation and host envelope | flake/check definitions present; opt-in NixOS modules/operator VM present | partial; blocked in this env for authoritative Nix execution | `nix/nixos/modules/*`, `nix/nixos/configurations/forge-operator-vm.nix`, `docs/status/nix_foundation_status.md`, `docs/runbooks/forge_operator_desktop_vm.md`, command outputs in `test_gap_analysis.md` | run Nix checks on a Nix-enabled host; keep host envelope opt-in and non-mutating |
| Fresh-clone boot integrity | core build path + VSA preflight scripts | real (guarded) | `scripts/check-vsa-files.mjs`, `scripts/forge-core.mjs`, `scripts/forge-smoke.mjs`, platform smoke scripts, tracked `services/core/internal/memory/vsa_*.go` | VSA status is authoritative source; maintain tracked files and strict preflight guard |

## Convergence highlights in this pass

1. Live raw backup restore is retired. Dry inspection is deterministic and integrity-bound; full backup now includes K20C/K20D authority state for inspection and future whole-store recovery without claiming live restore parity.
2. Retrieval runs/results/selection reasons now commit as one exact-scope FORGE-K evidence bundle without memory-observation or VSA duplication; retrieval usefulness and restore-outcome feedback are separate append-only utility evidence with rebuildable projections.
3. Legacy memory mutation boundaries are retired (`410 Gone`) with correlation/trace/workspace audit payloads, and legacy adapter ingress is removed.
4. Root core build/test/vet/core/smoke flows include strict VSA tracked-source preflight checks, and required VSA source files are tracked in authoritative branch state.
5. Model runtime M3/M4 is now real inside the governed modelruntime boundary: manifest/store/registry/backend/runtime service/APIs/config/audit paths are implemented with import/register flows, persistent lifecycle state, deterministic selection, backend expansion, runtime inspection endpoints, approval-required managed delete-file flow, disabled-by-default vLLM-compatible external endpoint support, policy-visible gateway `model.*` aliases, and governed chat/SSE streaming. Remaining work is hardening/supervision, not a FORGE-K live authority change.
