# Remaining Cutover Blockers

Date: 2026-04-22

This matrix is branch-scoped and verified against current code paths.

| Blocker | Severity | Exact files/modules involved | Current behavior | Ideal behavior | Smallest safe fix | Blocks Phase 6 / demos / deeper autonomy / deeper Nix |
|---|---|---|---|---|---|---|
| VSA source-state reproducibility (resolved context, not active blocker) | resolved-note | `services/core/internal/memory/vsa_engine.go`, `services/core/internal/memory/vsa_indexer.go`, `services/core/internal/memory/vsa_signals.go`, `scripts/check-vsa-files.*`, `scripts/forge-core.*`, `scripts/forge-smoke.*` | VSA status is authoritative source (not generated, not optional) and files are git-tracked in this branch state; strict tracked-source preflight remains active across root core/build/test/vet/smoke commands. | Keep VSA dependency authoritative and reproducible for fresh clones. | Keep required VSA sources tracked; keep strict preflight as guardrail. | no / no / no / no |
| VSA-derived sections are export-only by policy | resolved-policy | `services/core/internal/backup/service.go` (`pickSections`, `restoreExportOnlyReasons`), `services/core/internal/backup/service_test.go`, `docs/status/backup_export_coverage.md` | `full_backup` exports VSA sections; restore marks them `unsupported` + `exportOnly` with explicit policy warning and skipped counts. | Preserve deterministic export-only posture until a controlled derivation-safe import mode exists. | Keep export-only reporting/tests; require recompute/reindex flow after restore. | no / no / no / no |
| Legacy adapter compatibility ingress retirement | resolved | `services/core/internal/api/server.go`, `services/core/internal/api/server_adapters_test.go`, `apps/desktop/src/lib/api.ts`, `docs/status/legacy_adapter_route_report.md` | `/api/adapters/{id}/invoke` route removed from router; desktop adapter invoke path uses `/api/gateway/invoke`. | Keep gateway as sole execution authority with no alternate ingress. | Maintain gateway-path tests for `legacy.adapter.invoke`; do not re-add direct adapter route. | no / no / no / no |
| Trace/explain chain is still uneven for artifact-centric operations | medium | `services/core/internal/api/workspace.go`, `services/core/internal/api/phase5.go`, `services/core/internal/api/trace_report.go`, `services/core/internal/api/server.go`, `services/core/internal/gateway/service.go`, `docs/architecture/traceability.md` | `GET /api/audit/trace/{correlationId}` now returns one correlation-first report spanning gateway invocation rows, audit rows, artifact rows, provenance/journal/artifact-ref rows, and explicit link edges; desktop/operator UX still does not fully expose that report graph. | Canonical chain is easy to follow by correlation in both API and operator UI: event/intent -> gateway/syscall -> audit -> artifact/object/provenance journal. | Keep report schema stable, then wire operator-facing inspection UI/workflows to the consolidated report instead of stitching multiple pages manually. | conditional / no / conditional / no |
| Restore transactionality is DB-scoped only | high | `services/core/internal/backup/service.go`, `services/core/internal/backup/service_test.go`, `services/core/internal/artifacts/service.go` | Restore is atomic for supported DB sections in one SQL transaction. Non-DB side effects (artifact file bytes, external side effects) are not globally rollback-managed. | Restore guarantees are explicit and non-misleading; non-DB limitations are surfaced in result/docs. | Keep DB transaction scope; report atomic scope and explicit non-DB warnings in restore result/docs. | conditional / yes / yes / no |
| Rule-agent coverage remains narrow | medium | `services/core/internal/aios/autonomy/rule_agents.go`, `docs/architecture/rule_based_agents.md`, `docs/status/implementation_matrix.md` | Runtime has two rule agents (`OpenLoopStalenessAgent`, `CleanupProposalAgent`) and stays propose-only with safe warnings. Broader deterministic agents are explicitly deferred. | Coverage matches docs, or docs stay explicit about narrow scope. | Keep narrow safe scope and truthful docs; add new agents only with deterministic signal, policy, test, and trace coverage. | no / conditional / conditional / no |
| Compute seam/runtime split remains | medium | `services/core/internal/aios/compute/*`, `services/core/internal/aios/computelane/*`, `docs/status/implementation_matrix.md` | Compute abstractions remain split between seam/runtime packages; no broader unification work done in this phase. | One clear compute runtime story with explicit ownership boundaries. | Keep as documented deferred blocker; avoid broad restructuring in cutover hardening phase. | conditional / no / no / no |
| Nix checks blocked in this environment | medium | `flake.nix`, `nix/*`, `docs/status/test_gap_analysis.md`, `docs/status/current_baseline_gate.md` | Nix checks/build remain non-authoritative here due host daemon/feature availability constraints. | Nix checks run in authoritative Nix-capable environment and results are published. | Keep status honest and environment-scoped; do not treat Nix results from this host as authoritative. | no / no / no / yes |

## Verification Evidence

- Missing VSA compile failure (simulated absent files): `go test ./internal/memory ./internal/retrieval ./internal/api ./internal/jobs` fails with undefined VSA method symbols.
- VSA export-only policy: `restoreExportOnlyReasons` in backup service + explicit `exportOnly`/`unsupported` tests and warning coverage.
- Legacy adapter boundary: compatibility route removed; adapter invocation tests verify gateway-only execution (`legacy.adapter.invoke` via `/api/gateway/invoke`) and removed-route `404`.
- Tool ingress convergence proof: `docs/status/tool_execution_ingress_proof.md` inventories API/chat ingress points and confirms gateway-governed execution.
- Restore scope: single DB transaction in `RestoreBundle`; non-DB side effects not transaction-managed.
- Rule-agent scope: only `OpenLoopStalenessAgent` and `CleanupProposalAgent` implemented; broader maintenance agents are deferred in `docs/architecture/rule_based_agents.md` and `docs/status/implementation_matrix.md`.
